#ifndef LIBRARIES_H_INCLUDED
#define LIBRARIES_H_INCLUDED

#include <iostream>
#include <fstream>
#include <list>
#include <vector>
#include <string>

using namespace std;

const int N = 3;

bool exact_cover(int[], int[][N], int, int);

#endif // LIBRARIES_H_INCLUDED
